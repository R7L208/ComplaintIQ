.cache

build/
data/

compile_commands.json

# Python / uv virtual environment and notebook cruft.
# requirements.txt is intentionally NOT ignored: it is the committed lockfile.
.venv/
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# Developer-only local Make overrides (e.g. private PyPI index URL).
local.mk

# Databricks Asset Bundle local state / deploy artifacts.
.databricks/

# Local scratch space (notes, throwaway work).
scratch/

# MLflow local tracking store (runs, registry). Regenerated by the trainer.
mlruns/
mlflow.db
dist/
*.egg-info/

# Editor / OS cruft that should never be tracked
.DS_Store
.isaac/
